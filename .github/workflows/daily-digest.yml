# LEGACY: Grok Tasks email archive path — DISABLED as primary.
# Primary workflow: daily-x-digest.yml (twitter-cli + DeepSeek + Pages)
#
# Uncomment "on:" below only if you still want email-preview archival.
name: Legacy Grok Email Digest (disabled)

on:
  workflow_dispatch:
    # manual only — not scheduled

permissions:
  contents: write

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "Primary pipeline is daily-x-digest.yml (X cookie + DeepSeek)."
          echo "This legacy Grok-email job is not scheduled."
          exit 0
