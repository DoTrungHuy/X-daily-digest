# Core pipeline (X free cookie path + DeepSeek + site)
pyyaml>=6.0
# twitter-cli is optional install; see docs/SETUP_X_DEEPSEEK_PAGES.md
# pip install twitter-cli

# Legacy Grok-email path (optional)
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
playwright>=1.40.0
